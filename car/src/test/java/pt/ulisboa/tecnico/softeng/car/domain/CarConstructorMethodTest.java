package pt.ulisboa.tecnico.softeng.car.domain;

import org.junit.After;
import org.junit.Assert;
import org.junit.Before;
import org.junit.Test;

public class CarConstructorMethodTest{
	private static final String plate = "AA-09-BB";
	private /*static final*/ int kilometers = 0;
	private RentACar rentAcar; 
	
	@Before
	public void setUp() {
		//cria o rentAcar aqui
	}
	
	@Test
	public void success() {
		//Car car = new Car(plate,kilometers,RentACar );

		/*Assert.assertEquals(HOTEL_NAME, hotel.getName());
		Assert.assertTrue(hotel.getCode().length() == Hotel.CODE_SIZE);
		Assert.assertEquals(0, hotel.getNumberOfRooms());
		Assert.assertEquals(1, Hotel.hotels.size());*/
	}
	
	
	
	
	
}